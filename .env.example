# Copy to .env.local and fill in. Nothing here is required to run the MVP scaffold
# with the LocalRepository data layer.

# Public base URL, used for canonical URLs, sitemap.xml and robots.txt.
NEXT_PUBLIC_SITE_URL=https://uaedatacompliance.com

# --- Phase: swap LocalRepository -> SupabaseRepository (src/data/index.ts) ---
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=

# --- Admin panel (very basic single-user auth for MVP) ---
# ADMIN_PASSWORD=

# --- Lead / submission notifications (later) ---
# RESEND_API_KEY=
# LEADS_NOTIFY_EMAIL=
